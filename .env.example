# HomeBase environment variables
#
# Copy this file to .env.local for local development (that file is gitignored).
# No variables are required yet — this file documents them as they're introduced:
#
# - DATABASE_URL          Postgres connection string (added with MAD-86: Drizzle ORM setup)
# - BLOB_READ_WRITE_TOKEN Vercel Blob token for photo/document attachments (added with MAD-96)
